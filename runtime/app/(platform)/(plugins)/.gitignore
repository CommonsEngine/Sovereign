# Generated by scripts/generate-registry.ts — composed plugin route segments
# (symlinks in dev, copies in prod). Never commit these. The source of truth is
# always plugins/<id>/app/. This .gitignore anchors the route group so the
# App Router can compose default-shell plugins under the platform shell.
*
!.gitignore
