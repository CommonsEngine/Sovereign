export const events = {
  publish(_eventName: string, _payload?: unknown): void {
    // Placeholder implementation.
  },

  subscribe(_eventName: string, _handler: (payload: unknown) => void): void {
    // Placeholder implementation.
  },
};