{
  "name": "sovereign",
  "private": true,
  "version": "0.7.3",
  "workspaces": [
    "platform",
    "plugins/*"
  ],
  "scripts": {
    "bootstrap": "yarn install",
    "build": "yarn workspaces run build",
    "dev": "yarn workspace @sovereign/platform dev"
  }
}
